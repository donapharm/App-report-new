node_modules/
dist/
# Thư mục build tạm khi auto-deploy tráo bản (swap) + khoá chống chạy chồng
web/dist.new/
web/dist.old/
.auto-deploy.lock
.auto-deploy.last
.auto-deploy.dirty-since
.env
.env.bak*
.env.local
*.log

# Dữ liệu sinh/runtime (chạy `npm run seed` để tạo lại)
server/data/*.json
server/data/uploads/
# Dữ liệu auth runtime (phiên/thiết bị/mapping/audit) — TUYỆT ĐỐI không commit
server/data/auth/
# Output materialize doanh thu (bot server tự SINH LẠI mỗi kỳ) — KHÔNG commit để tránh
# working tree dirty làm KẸT auto-deploy. App không đọc artifacts/, chỉ là output phân tích.
artifacts/*materialize*
# Backup runtime (PM2/dữ liệu) — TUYỆT ĐỐI không commit (có thể chứa secret/key)
backups/
*.backup
verification-screenshots/
# Giữ lại file Excel mẫu để test tính năng Upload
!server/data/sample_upload.xlsx
