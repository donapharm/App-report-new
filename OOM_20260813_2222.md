# OOM 2026-08-13 22:22 GMT+7 — hồ sơ read-only

## Kết luận ngắn

Process `app-report` của release `3a3a47d8ac2634ffd0bdecfb46f71db24667a823` chết do **V8 JavaScript heap out of memory** lúc `22:22:16 GMT+7` ngày 13/08/2026. PM2 khởi động process thay thế PID `2046520`; restart count chuyển `371 → 372` theo baseline vận hành đã chụp.

Sự kiện xảy ra sau khi release đã chạy ổn định nhiều giờ, không nằm trong cửa sổ giám sát của một cutover mới. Theo quyết định kiến trúc của CEO, sự kiện này được giữ làm hồ sơ điều tra rò rỉ; nó **không phải lý do rollback candidate `9014644` chưa deploy**.

## Bằng chứng OOM

PM2 daemon ghi đúng thời điểm:

```text
2026-08-13T22:22:16: PM2 log: App [app-report:9] exited with code [0] via signal [SIGABRT]
2026-08-13T22:22:16: PM2 log: App [app-report:9] starting in -fork mode-
2026-08-13T22:22:16: PM2 log: App [app-report:9] online
```

Đoạn cuối V8 của PID cũ `784273`:

```text
Scavenge (reduce) ... 2045.2 ... MB ... allocation failure
Mark-Compact (reduce) 2047.1 ... -> 2047.0 ... MB
FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory
```

Native stack đi qua `v8::internal::JsonParser...ParseJson`, tức lỗi cuối xuất hiện khi cấp phát trong đường parse JSON. Hồ sơ này chưa đủ để kết luận object/corpus nào bị giữ lâu hoặc đâu là nguyên nhân rò rỉ; đó là việc điều tra riêng sau khi T08 an toàn.

- PID chết trong log V8: `784273`.
- PID PM2 khởi động sau OOM: `2046520`.
- Restart count vận hành: `371 → 372`.
- Release đang chạy khi OOM: `3a3a47d8ac2634ffd0bdecfb46f71db24667a823`.
- Health sau PM2 restart theo handoff vận hành: HTTP `200`.

## RSS được guard giữ lại

Guard read-only lấy mẫu 15 phút và ghi thêm các lần vượt ngưỡng:

| GMT+7 | PID | RSS |
|---|---:|---:|
| 20:13:35 | 784273 | 1.033.523.200 B (~0,96 GiB) |
| 20:28:41 | 784273 | 1.091.588.096 B (~1,02 GiB) |
| 20:45:59 | 784273 | 1.140.637.696 B (~1,06 GiB) |
| 20:47:59 | 784273 | 1.773.592.576 B (~1,65 GiB), cảnh báo 1,6 GiB |
| 21:01:04 | 784273 | 1.088.999.424 B (~1,01 GiB) |
| 21:16:10 | 784273 | 1.174.626.304 B (~1,09 GiB) |
| 21:22:12 | 784273 | 2.244.059.136 B (~2,09 GiB), bắt đầu cửa >1,8 GiB |
| 21:23:12 | 784273 | 1.159.438.336 B (~1,08 GiB), cửa được reset |
| 21:31:15 | 784273 | 1.188.646.912 B (~1,11 GiB) |

Chuỗi guard được giữ lại **không phủ đủ ba giờ và kết thúc trước OOM khoảng 51 phút**. Vì vậy không được nội suy RSS ngay sát 22:22 từ các mẫu trên. Dấu vết V8 tại thời điểm chết mới là bằng chứng trực tiếp heap đã chạm giới hạn khoảng 2.047 MB.

## Heap snapshot và giới hạn hiện hành

- Không tìm thấy file `*.heapsnapshot`, `Heap.*` hoặc heap dump liên quan.
- PM2 metadata của process trước rollback: `node_args=[]`.
- Không có `NODE_OPTIONS` trong PM2 environment đã lưu.
- Không thấy `--max-old-space-size` được đặt cho process.
- PM2 có `max_memory_restart='2G'`; đây là ngưỡng giám sát/restart của PM2, không phải bằng chứng Node được cấp một `--max-old-space-size` tùy chỉnh.

## Thiệt hại T07

Ba admin GET read-only trước OOM (`20:24`, `20:34`, `20:44 GMT+7`) đều trả:

- HTTP `200`;
- `2.091` dòng / `21` nhân viên;
- unavailable `0`, stale `0`;
- serving class `healthy_cached`;
- nhãn `Hiện 2.091/2.091 dòng`;
- lớp tổng sau phạt fail-closed với `18/21` nhân viên có số phạt.

Khi PID `784273` chết, **toàn bộ RAM cache gắn với process đó chắc chắn chết theo process**. Đây là mốc kỹ thuật “cache T07 đã chết”.

Tuy nhiên không có authenticated admin GET T07 nào được lưu trên PID mới `2046520` trước competing rollback lúc `23:09`. Khi việc xác minh được giao, PROD đã trỏ sang `7870f10`. Do đó các trường sau OOM trên exact `3a3a47d` phải ghi trung thực là:

- số dòng còn lại: **[blocked — không có mẫu post-restart trước rollback]**;
- số nhân viên còn lại: **[blocked — không có mẫu post-restart trước rollback]**;
- nhãn thiếu sau restart: **[blocked — không có mẫu post-restart trước rollback]**;
- serving class sau restart: **[blocked — không có mẫu post-restart trước rollback]**.

Không dùng trạng thái của `7870f10` để suy ngược trạng thái PID `2046520`.

## Drift runtime ngoài phiên điều tra

Evidence rollback cho thấy lúc `23:09:25–23:09:31 GMT+7`, một thao tác khác đã:

- đổi `current` từ release `3a3a47d` sang release `7870f10`;
- restart riêng `app-report`;
- để `app-report-tgbot` ở release cũ.

Lúc kiểm tra read-only `06:25` ngày 14/08, public `/version.json` vẫn là `7870f10-20260810-002828-161`. Phiên lập hồ sơ này không thực hiện rollback, restart hay thay đổi runtime.

## Evidence và SHA-256

- `artifacts/rollback-oom-to-7870f10-20260813-230925/app-report-error-before.log`
  - `31961309b599635e5cc821cfa0133b5fbefbc1a7b08bbf3ccfcb34c891a1e3e8`
- `artifacts/app-report-rss-guard-20260813/rss-15min.tsv`
  - `765ffd864f51096e71881713c020c62902c2fabf76f6c96abdaaeaefc74a3e7a`
- `artifacts/app-report-rss-guard-20260813/critical-window.tsv`
  - `38535c409bdcca79a2fe2bbe141337b4f87293ec66341cf76150588ce473587c`
- `artifacts/rollback-oom-to-7870f10-20260813-230925/rollback.log`
  - `2518a5dd9ffb4cdd92ab898a4cdac147cc53ceedfb95d98f58e98cfad08daecd`
- `artifacts/t07-prod-readonly-3x10m-20260813.json`
  - `f718000ea08a21ebee19a7286834b60093cb5dc0f8e345776726442fb3e38ea8`

Không có secret, cookie, token, OTP hoặc payload tài chính được chép vào tài liệu này.
