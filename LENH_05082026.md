# LỆNH SÁNG 05/08/2026 — mỗi bot đọc đúng phần của mình

> CEO giao Claude điều phối. Bot vào đây nhận việc, làm xong báo lại **kèm bằng chứng**, đừng báo miệng.
> Mọi mốc giờ trong file này là **giờ Việt Nam (GMT+7)**.

**Trạng thái lúc 06:00 ngày 05/08:** App Report PROD = `9fab0c4` = đầu `main`, không còn gì chờ deploy.

---

# PHẦN A — BOT DATAHUB

## A1. ‼ VIỆC GẤP NHẤT: đếm trước khi xử 2.600 việc tồn

Bạn báo outbox còn **2.600 event chờ replay**. **Trước khi cho chạy**, làm đúng một việc:

> **Đếm xem trong 2.600 event đó có bao nhiêu cái ghi vào kỳ `06.2026` hoặc `07.2026`.**

Vì sao: hai kỳ này **đã khoá sổ**, số đã chốt và đã dùng để tính thưởng/phạt trả cho nhân viên:

| Kỳ | Doanh thu | Số dòng |
|---|---|---|
| 06.2026 | **28.403.136.096đ** | **2.001** |
| 07.2026 | **30.917.892.673đ** | **2.016** |

- Đếm ra **0** → báo lại rồi cứ chạy bình thường.
- Đếm ra **khác 0** → **DỪNG, báo trước**, đừng chạy rồi mới nói. Chạy xong mới phát hiện thì không lần ngược được nữa.

**Sau khi chạy xong**, chạy tiếp lệnh này trong repo `donapharm/App-report-new` và **dán nguyên văn kết quả**:

```
node server/scripts/verify_frozen_periods.js
```
Mã thoát `0` = khớp · `1` = **LỆCH, dừng ngay** · `2` = chưa đọc được số (chưa kết luận được).

## A2. Khoá `vault-audit.lock` — làm cho nó tự nhả

Hôm qua bạn **gỡ tay** cái khoá mồ côi. Việc đó đúng và an toàn, nhưng lần sau máy khởi động lại giữa chừng thì y hệt sự cố cũ (01/08: 21 NV hiện 0đ; 03/08: 8 NV luân phiên mất số).

Thuật toán đầy đủ đã viết sẵn: **`DIRECTIVE_DATAHUB_VAULT_LOCK_SELFHEAL.md`** trong repo này. Tóm tắt: khoá phải ghi rõ **ai đang giữ (`pid` + `host`) và giữ từ lúc nào (`at`) + hạn `ttlMs` 15 giây**; chủ chết hoặc quá hạn thì **người sau tự phá khoá** và ghi nhật ký; việc chạy lâu thì **gia hạn**; thả khoá thì **chỉ thả đúng khoá của mình**.

**Trả lời 2 câu:**
1. Đã code phần tự nhả này chưa? (gỡ tay **không** tính là xong)
2. Khoá đó nằm ở repo/thư mục nào? Bên App Report tra `data-hub-smart-app` không thấy.

Sửa xong thì chạy **4 bước nghiệm thu** ở mục 5 của file directive và gửi kết quả từng bước. (Lần trước script của bạn đọc sai key `.env` nên **chưa bước nào chạy được** — không phải lỗi DataHub, nhưng cũng nghĩa là chưa có bằng chứng gì.)

---

# PHẦN B — BOT SERVER (App Report)

## B1. Map Telegram cho DN012 — đang chờ chính chị ấy

**Chỉ map DN012 (Đặng Thị Hồng Hạnh).** Bốn người còn lại (DN004 · DN021 · DN023 · VP004) **chờ CEO yêu cầu, KHÔNG tự map.**

Bạn báo chưa có Telegram ID xác thực — **đúng, và không được đoán.** CEO sẽ nhắn DN012 mở Telegram bấm **Start**. Sau khi chị ấy nhắn, ID hiện ra thì map ngay và báo lại.

## B2. Trước 09/08 — bật nhắc tin

Bật `EMP_COST_NOTIFY` và `BONUS_NOTIFY`. **Chạy `dryRun` ít nhất một lần trước**, dán danh sách sẽ nhắn cho ai rồi mới bật thật.

## B3. Sau 08/08 — ba việc còn tồn

- Cắm `runDueJobs()` (`scheduledJobs.js`) vào cron, chạy `dryRun` trước.
- Cắm `classifySyncExceptions()` vào materializer rồi `syncExceptionStore.write()`; dừng khi `balanced === false`. Danh mục 14 mã lý do có sẵn ở `syncExceptionCatalog.js`, **không phải tự nghĩ**.
- Handler AI đề xuất target **08:00 ngày 01** hằng tháng (GMT+7), tính lại ngày 09, **cấm tự áp**.

## B4. Làm rõ: commit `9986f0a` nằm ở repo nào?

Claude tra `git cat-file` trong repo App Report: **không tồn tại**. Nhiều khả năng nó ở DataHub, hoặc số hiệu ghi sai. Xác nhận lại rồi báo.

## B5. Quy tắc báo cáo từ nay

- **Luôn dán dòng version công khai** sau khi deploy (như lần `9fab0c4` — rất tốt, giữ nguyên cách đó).
- Deploy PASS mà nghiệm thu màn hình FAIL thì **ghi rõ "deploy PASS · acceptance FAIL"**, đừng gộp thành "xong".

---

# PHẦN C — NGƯỜI (không phải bot) · HẠN 08/08, CÒN 3 NGÀY

| # | Việc | Ai làm |
|---|---|---|
| C1 | Xác nhận khoản **3.995.000đ** MISA đang ở trạng thái "Đề nghị ghi" — ghi hay huỷ | **Kế toán** |
| C2 | Gán lại đơn **`DH479816174`** (MISA `341964` · Pizar-3 · `1.795.600đ` · `120.HTNT-PHARMACITY`) — đang gán cho **VP018 là telesaler**, phải gán cho **NV Sale hợp lệ** | **App Sale** |

Xong C2 thì App Report và App Sale khớp **0đ tròn**.

---

# Xếp theo mức nguy hiểm THẬT — CEO hỏi đúng

| Việc | Nguy hiểm thật? | Vì sao |
|---|---|---|
| **A1** đếm 2.600 event | **CÓ — cao nhất** | Chạy sai là **đổi số tháng đã khoá sổ**, sai thưởng đã trả cho nhân viên, và **không lần ngược được**. Đếm chỉ mất 5 phút. |
| **A2** khoá tự nhả | Trung bình | Đã xảy ra 2 lần thật. Nhưng App Report nay chịu được: dùng số cũ kèm nhãn, không hiện 0đ oan, không treo. Là **phiền lặp lại**, không phải mất tiền. |
| **C1 · C2** | Thấp nhưng có hạn | Số nhỏ (1,79tr và 3,99tr), nhưng để quá 08/08 thì kỳ khoá sổ và **không sửa được nữa**. |
| **B2 · B3** | Thấp | Việc dựng thêm tính năng, chưa bật cũng không hỏng gì. |

**Nói thẳng:** hôm qua em xếp cả 4 câu hỏi DataHub ngang hàng nhau nên nhìn như 4 vấn đề. Thực ra chỉ **A1** là gấp; A2 là gốc rễ nhưng đã giảm đau được; hai câu còn lại chỉ là **thành phần của A2**, không phải việc riêng.


---

# ĐỢT 2 — CEO lệnh 05/08 sáng: LÀM XONG 4 VIỆC HÔM NAY

> ✅ Rủi ro DataHub đã đóng lúc 07:14 (khoá tự nhả `de9edb7`, nghiệm thu 4/4, T06/T07 khớp).
> Bốn việc dưới là phần còn lại. **Việc nào chặn ở người thì bot phải CHUẨN BỊ SẴN để CEO chỉ gật một câu**, đừng để nguyên chữ "chờ ai đó quyết".

## V1 · Dòng cách ly `DH479816174` — hạn 08/08

Đơn MISA `341964` · Pizar-3 · **1.795.600đ** · đơn vị `120.HTNT-PHARMACITY`, đang gán **VP018 (telesaler)** nên bị loại khỏi doanh thu NV Sale. Xong việc này thì App Report và App Sale khớp **0đ tròn**.

**Bot server làm ngay, KHÔNG chờ ai:**
1. Tra xem **các đơn khác của đơn vị `120.HTNT-PHARMACITY`** trong T06–T08 đang thuộc NV Sale nào. Nếu chỉ có một người → đó là ứng viên rõ ràng.
2. Tra bảng phân công đơn vị hiện hành xem `120.HTNT-PHARMACITY` thuộc tuyến của ai.
3. **Báo lại 1 cái tên đề xuất kèm căn cứ** (ví dụ: *"DN0xx — đang giữ 14/15 đơn khác của đơn vị này"*). CEO gật là gán.
4. Hai nguồn mâu thuẫn nhau, hoặc không tra ra ai → **nói thẳng "không xác định được"** kèm số liệu đã tra. **Cấm đoán.**

## V2 · Khoản `3.995.000đ` MISA "Đề nghị ghi" — hạn 08/08

**Bot server chuẩn bị trước khi hỏi kế toán:** in ra **mã đơn · ngày · đơn vị · mặt hàng · NV đang gán · lý do đang ở trạng thái "Đề nghị ghi"**. Kế toán chỉ trả lời **GHI** hay **HUỶ** — không phải đi tra lại từ đầu.

Quá **08/08** là kỳ khoá sổ, **không sửa được nữa**.

## V3 · Bật `EMP_COST_NOTIFY` / `BONUS_NOTIFY` — CEO cho làm sớm hơn 09/08

Hai lỗi từ diễn tập đã sửa ở **`b49e585`**. Thứ tự bắt buộc:

1. Pull `b49e585`, deploy.
2. **Dọn config drift TRƯỚC** — `.env` đang ghi cờ `=1` trong khi PM2 chạy `=0`. Không dọn thì restart là notify **tự bật ngoài ý muốn**.
3. **Chạy lại `dryRun`**, kiểm đúng 2 điểm:
   - Danh sách **không còn** DN004 · DN005 · DN012 · DN017 (đó là các tin 0đ).
   - Mốc ngày in ra là **05/08**, không phải 04/08.
4. **Dán danh sách mới cho CEO xem** rồi mới bật thật. Danh sách sạch thì bật ngay sáng nay, không chờ 09/08.

‼ Bước 4 không được bỏ: đây là tin **tiền** gửi tới 17 người, sai một cái là 17 người đọc số sai.

## V4 · Khoá dùng chung `639e37e3` — làm 4 bước, KHÔNG cắt một nhát

1. Cấp khoá ping **mới** cho App Sale — **thêm vào, chưa gỡ `639e37e3`**. Hai khoá cùng chạy.
2. App Sale chuyển sang khoá mới, **xác minh chạy bình thường** (doanh thu vẫn về, mirror không đứt).
3. Xem nhật ký **đủ 24 giờ**, xác nhận `639e37e3` không còn ai gọi ngoài App Report.
4. Lúc đó mới gỡ quyền dùng chung và deploy `b238a9e`.

**Tuyệt đối không gỡ khoá cũ trước bước 2.** App Sale mất khoá là **đứt luồng doanh thu** — thứ đắt nhất trong cả hệ thống này.

Bước 2 cần người bên App Sale, nên **V4 có thể không xong trong hôm nay** — đó là giới hạn thật, báo sớm còn hơn hứa liều. `b238a9e` chưa lên cũng không sao: trong đó là bản sửa notify, mà V3 đã đi đường khác.

---

## Thứ tự làm

**V3 trước** (đang chặn tin tiền) → **V1 song song** (tra dữ liệu, không chờ ai) → **V2** (in bảng cho kế toán) → **V4** (chạy nhiều ngày).

---

# CHỐT 08:50 ngày 05/08 (giờ VN) — Claude đã tự tra branch, KHÔNG tin báo miệng

Hai nhánh đã lên origin, Claude fetch và đọc thật:
`origin/candidate/viec4-appreport-1ba8f44-20260802-214655` = `aa5e1b4` ·
`origin/review/viec4-assertion-only-54365b0` = `54365b0`.
Hai nhánh **chỉ khác nhau đúng 1 dòng** trong `server/test/employeeCostAllDeadline.test.js`.
Còn `b238a9e` thì **không có object ở bất kỳ đâu** — coi như không tồn tại, đừng nhắc nữa.

## ⛔ KHÔNG DUYỆT chặn bộ nhớ (`aa5e1b4` / `54365b0`) — 5 điểm, có bằng chứng

**1. Chính số đo của bạn đã kết tội nó.** Luồng 2 ⇒ **6/21 NV xong, 15 NV bị cắt**;
luồng 6 ⇒ **18/21 xong, 3 bị cắt**. 15 NV hiện "chưa lấy được số" **chính là triệu chứng
sự cố 01/08** mà cả tuần vừa rồi đi chữa. Đổi một sự cố lấy đúng sự cố đó là không đổi.

**2. Không có đường lùi.** `buildConcurrency()` dùng `boundedInteger(value, 2, 1, 2)` —
`max` = 2. Nghĩa là `.env` ghi `EMPLOYEE_COST_ALL_BUILD_CONCURRENCY=6` **vẫn ra 2**.
Chỉnh chỉ hạ được, không nâng được. Ra PROD thấy đau thì không có nút nào để vặn,
phải deploy lại. Cổng an toàn phải nới được, không thì nó là bê tông.

**3. Đẻ ra một kiểu hỏng MỚI, nặng hơn kiểu đang chữa.** `assertMemoryBudget` chạy
**trước** cả catalog fan-out, ngưỡng nhận việc = 768 − 192 = **576 MiB**; vượt là ném
**HTTP 503 `EMPLOYEE_COST_ALL_MEMORY_PRESSURE`** cho **cả request**. Tức là màn "Tất cả NV"
— **màn mặc định của tài khoản CEO** — không còn "thiếu vài NV kèm nhãn" nữa mà là
**trắng bảng kèm lỗi đỏ**. Trước khi bàn tiếp, trả lời đúng một số:

> **RSS thật của tiến trình App Report trên PROD lúc này là bao nhiêu MiB?**
> (`pm2 describe` hoặc `process.memoryUsage().rss`, dán nguyên văn, kèm giờ VN.)

Nếu bình thường nó đã quanh 500–600 MiB thì cổng 576 MiB sẽ bật 503 gần như mỗi lần —
phải biết trước khi bật, không phải để CEO phát hiện hộ.

**4. `54365b0` KHÔNG phải "chỉ đổi tên", nó làm test nói dối.** Assertion
`EMPLOYEE_COST_ALL_CONCURRENCY >= 4` viết ra để canh **luồng dựng bảng 21 NV**. Sau khi đổi,
constant đó chỉ còn canh fan-out **nạp ứng lần 1** (`routes.js:1168`); luồng dựng bảng
dùng constant khác **bằng 2**. Claude đã chạy thử trên chính nhánh đó: **22/22 xanh** —
xanh mà thứ nó bảo vệ đã biến mất. Test xanh kiểu này còn tệ hơn test đỏ.

**5. Rác kèm theo** (nhỏ, nhưng cho thấy bản vá chưa soát lại): `routes.js:91`
`let memoGeneration = 0;` khai rồi **không ai dùng**; trường `generation` ghi vào mọi
entry memo nhưng **không chỗ nào đọc**. Dọn đi.

Ngoài ra `memoGet` bị **viết lại** (`hit.ttl` → `hit.ttlMs`, thêm `staleMs` theo entry,
dời mốc `t` sang lúc promise resolve). Claude đã tra: không còn chỗ nào đọc `.ttl` cũ nên
**không gãy** — nhưng đây là sửa lõi cache, không phải "chặn bộ nhớ". Lần sau tách commit.

## ✅ Muốn được duyệt thì chọn MỘT trong ba, kèm số đo mới

| Cách | Làm gì | Điều kiện bắt buộc |
|---|---|---|
| **(a)** *(Claude nghiêng về cách này)* | Nâng trần `buildConcurrency` lên **6** (`boundedInteger(value, 6, 1, 8)`) | Đo lại: ở luồng 6, RSS đỉnh khi dựng 21 NV là bao nhiêu MiB |
| **(b)** | Giữ luồng 2, **nâng `EMPLOYEE_COST_ALL_DEADLINE_MS`** cho đủ 21 NV | Trần cứng **≤ 40.000 ms**; đo lại phải ra **≥ 18/21 NV xong**, và phải chứng minh Cloudflare không cắt |
| **(c)** | Chặn RAM bằng cách khác (giới hạn kích thước payload, stream, xả bớt sau mỗi NV) | Vẫn giữ luồng ≥ 6 |

**Ba điều kiện chung, không được bỏ cái nào:**
1. Bảng 21 NV vẫn phải đi qua `mapWithDeadline` — quay về `mapWithConcurrency` là treo lại.
2. Assertion canh số luồng **ở lại**, và phải trỏ đúng constant mà luồng dựng bảng đang dùng.
   Sửa cho nó **đúng**, không phải sửa cho nó **xanh**.
3. Cổng 503 chỉ được bật khi đã dán RSS thật của PROD. Nếu chưa đo được thì **bỏ hẳn cổng 503**
   ở lần này, giữ phần giảm TTL lỗi (2 phút) — phần đó tốt, không tranh cãi.

Gói lại đúng một câu cho CEO gật (theo luật báo cáo mới):
> *"Sửa X · số đo Y/21 NV xong · lùi được bằng `git revert <sha>`."*

## ✅ DUYỆT V3 — bật `EMP_COST_NOTIFY` / `BONUS_NOTIFY` ngay hôm nay

Danh sách diễn tập đã sạch, đúng điều kiện đặt ra: **13 NV · 251.801.312đ**, không còn
DN004 · DN005 · DN012 · DN017 (các tin 0đ), mốc ngày in ra **05/08**. DN002 chỉ nhận
tin báo lỗi nguồn — đúng, vì không có số thì không được nhắn số.

Trình tự bật, **không đảo**:
1. Dọn config drift trước (`.env` đang `=1` trong khi PM2 chạy `=0`) — không dọn thì
   restart là tự bật ngoài ý muốn.
2. Bật thật.
3. **Sau khi bật, dán lại: đã gửi bao nhiêu tin · ai không nhận được · vì sao.**
   Gửi hụt mà im là lỗi nặng hơn không gửi.

DN012 vẫn chưa map Telegram — **không đoán ID**, cứ để chị ấy ngoài danh sách cho tới khi
chính chị ấy bấm Start.

## Việc còn lại giữ nguyên thứ tự

**V1** (đề xuất 1 cái tên cho `DH479816174`, kèm số đếm căn cứ, cấm đoán) →
**V2** (in bảng `3.995.000đ` để kế toán chỉ trả lời GHI/HUỶ) → **V4** (4 bước, nhiều ngày).
Hạn V1 · V2 là **08/08**, quá là kỳ khoá sổ.
