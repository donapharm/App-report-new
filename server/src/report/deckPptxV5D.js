'use strict';
const fs=require('fs');const path=require('path');const PptxGenJS=require('pptxgenjs');const {screenshotsFromHtml}=require('./deckPptx');
async function buildPptxV5D({htmlPath,outputPath,imageDir,title,expectedCount}){fs.mkdirSync(imageDir,{recursive:true});const htmlText=fs.readFileSync(htmlPath,'utf8'),dynamicCount=Number(expectedCount||(htmlText.match(/data-slide="/g)||[]).length);if(!dynamicCount)throw Error('V5D HTML has no slides');const rendered=await screenshotsFromHtml(path.resolve(htmlPath),{width:1920,height:1080,imageDir:path.resolve(imageDir),expectedCount:dynamicCount});if(rendered.files.length!==dynamicCount)throw Error(`V5D requires ${dynamicCount} slides, got ${rendered.files.length}`);const pptx=new PptxGenJS();pptx.layout='LAYOUT_WIDE';pptx.author='GROUP DONAPHARM — App Report';pptx.company='CÔNG TY CỔ PHẦN DONAPHARM';pptx.subject='CEO Premium Pharmaceutical Intelligence — DRAFT V5D CEO PHOTOS';pptx.title=title||'GROUP DONAPHARM — CEO Deck V5D CEO Photos';pptx.lang='vi-VN';pptx.theme={headFontFace:'Aptos Display',bodyFontFace:'Aptos',lang:'vi-VN'};for(const file of rendered.files){const slide=pptx.addSlide();slide.background={color:'003661'};slide.addImage({path:file,x:0,y:0,w:13.333333,h:7.5})}await pptx.writeFile({fileName:path.resolve(outputPath)});return{outputPath:path.resolve(outputPath),slideCount:dynamicCount,imageFiles:rendered.files,width:1920,height:1080,bytes:fs.statSync(outputPath).size}}
module.exports={buildPptxV5D};
