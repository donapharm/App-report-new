{
  "name": "app-report-new",
  "version": "2.0.0",
  "private": true,
  "description": "App Report New — smart report app (Donapharm)",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix web install && npm run seed",
    "seed": "node server/seed.js",
    "dev": "concurrently -n API,WEB -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
