# LỆNH 06/08/2026 — BỐN VIỆC NHỎ, CEO DUYỆT LÀM MỘT ĐỢT

> CEO: *"đồng ý làm xong luôn đi nào, để tiến hành các việc lớn và phức tạp hơn."*
> Mục tiêu: đóng nốt tầng việc nhỏ để dọn đường cho việc lớn (đọc file Excel thật, login OTP/SSO).
> Mọi mốc giờ là **giờ VN (GMT+7)**. PROD hiện tại: `636f9fc`.

---

## V-A · Khối cảnh báo bộ lọc bị ẩn khi thu gọn  🟢 nhỏ nhất, làm trước

**Lỗi:** `overview-filter-note` đang nằm **trong** `<div id="overview-filter-panel" hidden={!expanded}>` ⇒ thu gọn là mất câu *"Target không phân bổ theo lát cắt này nên App không tính % target sai."*

**Sửa:** đưa khối đó **ra ngoài** thẻ panel, đặt ngay sau nó, giữ nguyên điều kiện `activeCount > 0`.

**Test:** thêm **đúng 1 assert** kiểm `overview-filter-note` xuất hiện **sau** thẻ đóng của panel. Assert này **phải FAIL với code hiện tại** — viết xong chạy thử trên code cũ, không đỏ thì assert vô dụng, viết lại.

---

## V-B · Con mắt ẩn/hiện số tiền  🟢 nhỏ

Đọc **`SPEC_PRIVACY_EYE.md`**. Bốn điểm không được làm khác:

1. **Một công tắc cho cả app**, đặt ở thanh tiêu đề — không phải từng trang.
2. **Mặc định ẩn**, và **không nhớ trạng thái "đang hiện"** (F5 ⇒ về ẩn; cấm ghi `localStorage`).
3. **Tự ẩn** sau 60s không thao tác, và ngay khi `visibilitychange` sang ẩn — **dùng lại** cơ chế của chuông, không dựng cái thứ hai.
4. **‼ Đang ẩn thì KHOÁ nút Duyệt · Từ chối · Mở khoá · Ghi đã trả · Gỡ ghi nhận.** Không ai được duyệt tiền khi không nhìn thấy số. Backend vẫn chặn độc lập — ẩn nút không phải lớp bảo vệ.

**Tooltip bắt buộc:** *"Ẩn số trên màn hình — không phải khoá bảo mật."* Có test cấm mọi chữ "bảo mật/an toàn" mô tả tính năng này.

---

## V-C · Bật sống màn "Chưa đồng bộ"  🟡 ‼ CLAUDE ĐÍNH CHÍNH: KHÔNG PHẢI "CHỈ NỐI DÂY"

**Claude nói sai lúc rà soát.** Đã tra kỹ `materialize_july_revenue.js`:

- Dòng 378 `const sourceRows = [...misa, ...partner]` — nhưng **đây đã là dòng ĐƯỢC NHẬN**, không phải nguồn đầy đủ.
- `fetchCrmMirror` lọc sẵn `revenue_bucket <> 'excluded'`; `partnerPartition.includedRows` cũng chỉ là phần được nhận.

⇒ **Materializer không bao giờ nhìn thấy dòng bị loại.** Mà classifier cần đúng phần bị loại đó. Nên không thể "chỉ gọi thêm một hàm".

### Cách làm ĐƯỢC DUYỆT: script RIÊNG, không đụng materializer

Dựng **`server/scripts/build_sync_exceptions.js`** — **CHỈ ĐỌC nguồn, chỉ ghi `syncExceptionStore`**:

1. Lấy **lần đồng bộ MISA thành công mới nhất** của kỳ (dùng lại `LATEST_MISA_RUN_SQL`).
2. Truy vấn **TOÀN BỘ** dòng của kỳ — **KHÔNG** lọc `revenue_bucket <> 'excluded'`, **KHÔNG** lọc gì khác. Đây là điểm mấu chốt.
3. Lấy danh sách `includedLineIds` từ **slot đang active** của kỳ đó (dữ liệu materializer đã ghi).
4. `classifySyncExceptions({ period, sourceRows, includedLineIds, knownUnits, knownProducts, roster })`.
5. **Bất biến:** `Σ(đưa vào) + Σ(loại) == Σ(nguồn)`. **Lệch ⇒ DỪNG, thoát mã 1, KHÔNG ghi store.**
6. `balanced === true` mới `syncExceptionStore.write(period, ...)`.

**Vì sao KHÔNG sửa materializer:** đó là script **an toàn nhất repo** (canh kỳ khoá sổ, ba lớp bất biến, ghim doanh thu T06/T07). Thêm truy vấn vào đó **để phục vụ một màn báo cáo** là đánh đổi rủi ro lấy tiện lợi. Script riêng đạt cùng kết quả mà **không thể làm hỏng doanh thu**.

**Nghiệm thu:** chạy cho **T07**, mở màn "Chưa đồng bộ" phải có dữ liệu; mỗi mã lý do phải đủ **nghĩa · ai xử lý · làm gì** (lấy từ `syncExceptionCatalog`, **cấm nghĩ mã mới**); tổng phải cân.

---

## V-D · Cắm lịch chạy `runDueJobs()`  🟡 nhỏ nhưng phải cẩn thận

**Hiện trạng:** `scheduledJobs.js` đủ cả, nhưng **0 nơi gọi** — grep toàn `server/src` (kể cả `index.js`) và `server/scripts`.

**‼ Phân biệt hai bộ lịch, đừng gộp:**
- Lịch **gửi tin** chi phí/thưởng (12:30 T7 · cuối tháng 20:00 · ngày 9) do **`app-report-tgbot`** chạy — **ĐANG HOẠT ĐỘNG, KHÔNG ĐỤNG VÀO**.
- `runDueJobs()` là lịch **của App Report** — chưa từng chạy.

**Làm:**
1. **Chạy `dryRun` trước**, dán ra danh sách việc sẽ chạy và mốc giờ. Không dán thì không được bật.
2. Cắm chạy mỗi **~5 phút**. Ưu tiên **cron ngoài** (PM2/systemd gọi script) hơn `setInterval` trong tiến trình — tiến trình restart là `setInterval` mất, cron thì không.
3. Job `target_proposal` **chưa có handler** ⇒ để nó **không làm gì** và **ghi log**, tuyệt đối **không tự áp target**. Handler làm ở đợt sau.
4. Trạng thái "đã chạy" phải ghi vào state file để **restart không chạy lại lần hai**.

---

## Thứ tự và cách báo

**V-A → V-B → V-C → V-D.** Mỗi việc một candidate riêng, **tách từ đầu `claude/new-session-eifd44`** (đừng lặp lại vụ tách từ main cũ làm mất 7 file).

Mỗi việc báo đủ: **exact SHA đã push origin** · test focused + full server + web + build · và **một câu xin duyệt** theo mẫu:
> *"Sửa X · bằng chứng Y · lùi được bằng `git revert <sha>`."*

**Luật nghiệm thu đang hiệu lực, dùng bản MỚI NHẤT:**
- Cổng (b) chỉ FAIL khi ô `—` ở bản **mới** trong khi bản **cũ có số** cùng thời điểm.
- Lần tải đầu sau deploy được phép `—` (cache nguội); chờ ~1 phút làm mới mới kết luận.
- Rollback về **bản tốt gần nhất đã nghiệm thu**, không lùi về release cũ tuỳ ý.

## Việc lớn xếp sau đợt này

`SPEC_UPLOAD_REAL_FILE.md` (đọc file Excel thật) → handler AI đề xuất target → login OTP/SSO.
