# ✅ CLAUDE REVIEW — PASS: VP018 Telesaler + DN022 v3.7

Branch: `fix/dn022-separate-formula-20260731` (2 commit: `1cf17c5`, `302fac4`)
Review: Claude, 01/08/2026 tối. **Kết luận: ĐẠT — được merge.**
Đây là **cổng review** trong P0-3. Cổng này **ĐÃ MỞ** — bot không phải chờ Claude nữa, cứ rebase → merge → deploy khi có quyền thực thi.

## Đã kiểm gì

**1. Khoá công thức khớp** ✅
`employee_bonus_tiers.json` = `bonus-v3.7-dn022-separate-formula-r1` · `bonus_formula_lock.json` = `v3.7` + `sourceHash c86117…fb03`, đã đưa `employeeIncentivePolicy.js` vào `sources`. Test `bonusFormulaVersion` **XANH**.

**2. Test đầy đủ** ✅
Server **594 pass / 9 fail** — đúng **9 lỗi nền cũ** (3 `authTrustedDevice` thiếu fixture + 6 PDF thiếu `pdfinfo`), **KHÔNG lỗi mới**.
Targeted 18/18 XANH: `bonusFormulaVersion`, `employeeIncentivePolicy`, `revenueAttributionGuard`, `employeeRevenuePolicy`.

**3. Phạm vi chặn đúng người, đúng loại tin** ✅ — *đây là chỗ dễ sai nhất, đã soi kỹ*
- `SEPARATE_FORMULA_EMP_CODES = {DN022}` — chỉ DN022 ra khỏi công thức tiền.
- `XU_PENALTY_EMP_CODES = {DN002, DN004, DN022}` — **DN022 VẪN trong phép tính Xu** (đúng ý CEO: tách khỏi tiền, không tách khỏi Xu).
- `MONETARY_NOTIFY_BLOCKED_EMP_CODES = {DN002, DN004, DN021, DN022, DN023, VP004, VP018}`.

**4. ‼ KHÔNG chặn nhầm cảnh báo vận hành** ✅ — rủi ro lớn nhất, đã loại trừ
`employeeIncentivePolicy` **chỉ** được dùng bởi 4 module TIỀN: `employeeBonus`, `employeePenalty`, `bonusNotify`, `penaltyNotify`.
`syncAlert.js` và `highValueOrderAlert.js` **KHÔNG** tham chiếu tới nó ⇒ **VP018 vẫn nhận cảnh báo đồng bộ + đơn >50 triệu** (đúng thiết kế: VP018 là người sửa ngày giao, phải nhận tin vận hành dù bị chặn tin tiền).

## Lưu ý khi rebase
Rebase lên `origin/main` **conflict DUY NHẤT ở `CHANGELOG.md`** (hai bên cùng thêm mục ở đầu file) — **không có conflict code**. Giải bằng cách **giữ cả hai mục**, không bỏ bên nào.

## Sau khi merge phải kiểm trên production
1. DN022: màn chi phí ghi rõ *"đang chờ công thức riêng"*; **không** có số thưởng P1/P2, **không** có phạt target/C45; **vẫn** có phần Điểm/Xu.
2. VP018: không được phân bổ doanh thu `emp_code`; **doanh thu toàn công ty KHÔNG đổi** (phần của VP018 chuyển `UNALLOCATED` + `NON_SALES_ROLE_QUARANTINED`).
3. `formulaVersion` API trả **v3.7**.
4. Thử 1 cảnh báo vận hành → VP018 **vẫn nhận** (không bị chặn nhầm).

## Mốc chết
Phải merge + deploy **TRƯỚC 08/08**. Nếu tới 09/08 chưa xong ⇒ **KHÔNG được bật** `EMP_COST_NOTIFY`/`BONUS_NOTIFY`, vì VP018 và DN022 sẽ nhận **tin tiền sai** — gửi rồi không rút lại được.
